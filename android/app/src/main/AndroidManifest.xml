<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        Voice calling. RECORD_AUDIO is the microphone and is requested at
        runtime; the rest are what WebRTC needs to establish and keep a call.

        MODIFY_AUDIO_SETTINGS lets the SDK route audio to the earpiece rather
        than the speaker, which is the difference between a phone call and a
        speakerphone call held to your ear. BLUETOOTH_CONNECT is what makes a
        headset work on Android 12 and above.

        FOREGROUND_SERVICE_MICROPHONE is declared because Android 14 refuses a
        microphone foreground service without it, and a call that continues
        while the app is backgrounded is exactly that service.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <!-- The camera feature is deliberately NOT declared: this app never asks
         for one, and declaring a feature it does not use would narrow the
         device list on the Play Store for nothing. -->
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>

    <!--
        Phase 4: notifications. Android 13 (API 33) made this a RUNTIME
        permission; without it the system silently drops every notification and
        nothing reports an error. `firebase_messaging` requests it via
        PushMessaging.start().
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="jawwid_chat"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!--
                Phase 4: what makes a notification tap reach Flutter.

                The server sets `android.notification.click_action` to this exact
                string (FcmPushProvider.payload), and without a matching filter
                the tap opens the launcher activity with no payload attached, so
                `onMessageOpenedApp` never fires and the deep link has nothing to
                route on. The two halves have to agree; this is the other half.
            -->
            <intent-filter>
                <action android:name="FLUTTER_NOTIFICATION_CLICK"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
